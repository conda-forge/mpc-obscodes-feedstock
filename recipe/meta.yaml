{% set name = "mpc-obscodes" %}
{% set version = "2026.3.18" %}
package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/mpc_obscodes-{{ version }}.tar.gz
  sha256: 2003736b10559a80a112da1f6672bbcabb10600bdcd92edba352e13767918de7

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0

requirements:
  host:
    - python {{ python_min }}
    - setuptools >=45
    - wheel
    - pip
  run:
    - python >={{ python_min }}

test:
  imports:
    - mpc_obscodes
  commands:
    - pip check
  requires:
    - pip
    - python {{ python_min }}
about:
  summary: Minor Planet Center's Observatory Codes
  home: https://github.com/B612-Asteroid-Surveys/mpc_obscodes
  license: MIT
  license_file: LICENSE

extra:
  recipe-maintainers:
    - akoumjian
    - moeyensj
    - ntellis
    - KatKiker
